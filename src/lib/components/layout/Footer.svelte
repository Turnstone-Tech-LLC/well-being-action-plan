<footer>
	<div class="footer-content">
		<p class="copyright">
			&copy; {new Date().getFullYear()} Well-Being Action Plan
		</p>
		<p class="tagline">
			A shared protocol for patient well-being
		</p>
	</div>
</footer>

<style>
	footer {
		background-color: var(--color-primary);
		color: white;
		padding: var(--space-6) var(--space-4);
		margin-top: auto;
	}

	.footer-content {
		max-width: var(--max-width);
		margin: 0 auto;
		text-align: center;
	}

	.copyright {
		margin: 0;
		font-size: var(--font-size-sm);
	}

	.tagline {
		margin: var(--space-2) 0 0;
		font-size: var(--font-size-sm);
		opacity: 0.8;
	}
</style>
