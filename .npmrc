# Enforce pnpm as the only package manager
# This prevents accidentally using npm or yarn
engine-strict=true

# Prevent auto-install of peers
auto-install-peers=false

# Use the latest pnpm lockfile format
lockfile-version=9.0
